/**
 * useLookup — Re-export from the unified SmritiScreen Engine
 * @deprecated Use `useScreenSchema.ts` directly for new code.
 * This file kept for backward compatibility with existing imports.
 */
export { useLookup, type LookupOption, type SmritiField } from './useScreenSchema';
export { useScreenSchema, useCaptions, usePayFields, useTillFields } from './useScreenSchema';
